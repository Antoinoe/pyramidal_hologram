using System.Collections;
using System.Collections.Generic;
using Leap.Unity;
using UnityEngine;

public class LeapConnectionTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //print(FindObjectOfType<LeapServiceProvider>().IsConnected());
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
